# No third-party dependencies — uses only the Python standard library.
# Listed here for clarity and to allow future extension (e.g. matplotlib for
# sensitivity plots, scipy for KSG mutual-information estimation).
